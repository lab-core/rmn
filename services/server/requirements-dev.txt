# Test-only dependencies (install alongside requirements.txt to run pytest).
-r requirements.txt
# the contracts shared with the executor (services/common); the Docker image
# installs the same package from the build context
-e ../common
pytest==9.1.1
pytest-timeout==2.4.0
mongomock==4.3.0
fakeredis==2.37.1
