# Runtime dependencies of the executor (direct ones; pip resolves the rest).
# TensorFlow is not one of them: the digit model runs through LiteRT
# (python/process_copy/classifier.py). Training and export need
# requirements-train.txt.
ai-edge-litert==2.2.0
colorama==0.4.6
fpdf==1.7.2
img2pdf==0.6.3
imutils==0.5.4
matplotlib==3.11.1
numpy==2.5.3
opencv-python-headless==5.0.0.93
pandas==3.0.5
pdf2image==1.17.0
pillow==12.3.0
psutil==7.2.2
pymongo==4.18.0
PyMuPDF==1.28.2
pypdf==6.18.0
python-socketio==5.16.4
redis==8.1.0
# python-socketio's client opens with HTTP long-polling through requests; without
# it every executor dies at start-up ("namespaces failed to connect")
requests==2.34.2
seaborn==0.13.2
Unidecode==1.4.0
websocket-client==1.9.2
Werkzeug==3.1.8
